// Relies on PDFCreator setup as default printer and 'custom' printer that doesn't pop up a dialog box when trying to print

process all open docs

don't display in concert pitch

print dialog
button "Check All"
activate "Display colors"

button "Ok"

wait 5